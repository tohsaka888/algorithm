import { testArray } from "./data";

/**
 * 冒泡排序
 * @param {number[]} arr
 * @returns {number[]}
 */
const bubbleSort = <T = number>(arr: T[]): T[] => {
  //write your code here!

  return arr;
};

// run code
bubbleSort(testArray);
